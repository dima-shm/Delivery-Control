@model IEnumerable<DelControlWeb.Models.User>
@{
    ViewBag.Title = "Управление курьерами";
}

<table class="table table-striped table-hover">
    <tr>
        <th>Имя пользователя</th>
        <th>Номер телефона</th>
        <th>Адрес</th>
        <th>Email</th>
        <th>Статус</th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.UserName</td>
            <td>@item.Phone</td>
            <td>@item.Address</td>
            <td>@item.Email</td>
            <td><p style="color: green">@item.Status</p></td>
            <td>
                <a href="/Couriers/SelectOrder?companyId=@item.CompanyId&courierId=@item.Id" style="color: #12B2D4;">Назначить заказ</a>
            </td>
        </tr>
    }
</table>