@model IEnumerable<DelControlWeb.Models.User>
@{
    ViewBag.Title = "Управление менеджерами";
}

<p>
    @Html.ActionLink("Добавить менеджера", "Create")
</p>
<table class="table table-striped table-hover">
    <tr>
        <th>Имя</th>
        <th>Номер телефона</th>
        <th>Адрес</th>
        <th>Email</th>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.UserName</td>
            <td>@item.Phone</td>
            <td>@item.Address</td>
            <td>@item.Email</td>
            <td>
                <a href="/ManagerAccounts/Edit/@item.Id" class="glyphicon glyphicon-pencil" style="color: green; text-decoration: none" />
                <a href="/ManagerAccounts/Details/@item.Id" class="glyphicon glyphicon-eye-open" style="color: cornflowerblue; text-decoration: none" />
                <a href="/ManagerAccounts/Delete/@item.Id" class="glyphicon glyphicon-trash" style="color: red; text-decoration: none" />
            </td>
        </tr>
    }
</table>